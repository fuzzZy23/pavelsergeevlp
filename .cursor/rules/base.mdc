---
description: 
globs: 
alwaysApply: true
---
### 🔄 Осведомленность о Проекте и Контекст
- **Проверяй `TASKS.md`** перед началом новой задачи. Если задача не указана, добавь ее с кратким описанием и сегодняшней датой в формате dd.mm.yyyy.


### 🧱 Структура Кода и Модульность
- **Никогда не создавай файл длиннее 500 строк кода.** Если файл приближается к этому лимиту, проведи рефакторинг, разделив его на модули или вспомогательные файлы.
- **Организуй код в четко разделенные модули**, сгруппированные по функциональности или ответственности.
- **Используй четкие, согласованные импорты** (предпочитай относительные импорты внутри пакетов).

### 🧪 Тестирование и Надежность
- **Всегда создавай модульные тесты Pytest для новых функций** (функции, классы, маршруты и т.д.).
- **После обновления любой логики** проверь, нужно ли обновить существующие модульные тесты. Если да, сделай это.
- **Тесты должны находиться в папке `/tests`**, зеркально отражающей структуру основного приложения.
  - Включай как минимум:
    - 1 тест для ожидаемого использования
    - 1 тест на граничный случай
    - 1 тест на случай сбоя

### ✅ Завершение Задач
- **Отмечай выполненные задачи в `TASKS.md`** сразу после их завершения.
- Добавляй новые подзадачи или TODO, обнаруженные во время разработки, в `TASKS.md`.

### 📎 Стиль и Соглашения
- **Используй Python** в качестве основного языка.
- **Следуй PEP8**, используй аннотации типов и форматируй с помощью `black`.
- **Используй `pydantic` для валидации данных**.
- Используй `FastAPI` для API, если применимо.
- Пиши **докстринги для каждой функции**, используя стиль Google:
  ```python
  def example():
      """
      Краткое описание.

      Args:
          param1 (тип): Описание.

      Returns:
          тип: Описание.
      """
  ```

### 📚 Документация и Объяснимость
- **Обновляй `README.md`** при добавлении новых функций, изменении зависимостей или модификации шагов установки.
- **Комментируй неочевидный код** и убедись, что все понятно разработчику среднего уровня.
- При написании сложной логики **добавляй встроенный комментарий `# Причина:`**, объясняющий *почему*, а не только *что*.

### 🧠 Правила Поведения ИИ
- **Никогда не предполагай отсутствующий контекст. Задавай вопросы, если не уверен.**
- **Никогда не выдумывай библиотеки или функции** – используй только известные, проверенные пакеты Python, используя mcp context7.
- **Всегда подтверждай существование путей к файлам и имен модулей**, прежде чем ссылаться на них в коде или тестах.
- **Никогда не удаляй и не перезаписывай существующий код**, если это не указано явно или не является частью задачи из `TASKS.md`.
